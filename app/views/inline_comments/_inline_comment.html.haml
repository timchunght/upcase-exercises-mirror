%li
  %header
    = render 'avatar', user: inline_comment.user
    %span.username
      = inline_comment.user.username
      &ndash;
    %span.timestamp
      = time_ago_in_words(inline_comment.created_at)
      ago
  %span.comment
    :markdown
      #{inline_comment.text}
