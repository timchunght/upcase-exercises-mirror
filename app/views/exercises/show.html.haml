- content_for :page_title, @exercise.title

%article.main-inner
  %header
    %h1= @exercise.title

  = render 'intro', exercise: @exercise

  = button_to(                      |
    t('.start_exercise'),           |
    exercise_clone_path(@exercise), |
    method: :post                   |
  )                                 |
